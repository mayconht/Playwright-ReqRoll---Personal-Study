// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/dotnet
{
	"name": "C# (.NET)",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/dotnet:1-8.0",
	// "features": {
	// 	"ghcr.io/devcontainers/features/dotnet:2": {
	// 		"version": "latest"
	// 	},
	// 	"ghcr.io/schlich/devcontainer-features/playwright:0": {}
	// },

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [5000, 5001],
	// "portsAttributes": {
	//		"5001": {
	//			"protocol": "https"
	//		}
	// }

	// Use 'postCreateCommand' to run commands after the container is created.
	 "postCreateCommand": "dotnet restore && dotnet build && pwsh /workspaces/Playwright-ReqRoll/bin/Debug/net8.0/playwright.ps1 install-deps && pwsh /workspaces/Playwright-ReqRoll/bin/Debug/net8.0/playwright.ps1 install && dotnet test",

	 //Add extensions for C# development
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-dotnettools.csdevkit",
				"tal7aouy.rainbow-brackets",
				"ms-playwright.playwright",
				"alexkrechik.cucumberautocomplete"
			]
		}
	},
	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	"remoteUser": "root"
}
